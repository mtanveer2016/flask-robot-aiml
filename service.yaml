apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: flask-robot-monitor
  namespace: monitoring
  labels:
    release: kube-prometheus-stack  # Critical - must match Prometheus config
spec:
  selector:
    matchLabels:
      app: flask-robot
      release: kube-prometheus-stack  # Match service labels
  namespaceSelector:
    matchNames:
      - default
  endpoints:
    - port: http
      interval: 30s
      path: /metrics
